# TechSpace Device Manager

## Requirements

* NodeJS 5+
* MQTT Broker (Mosquitto)

## Installation

* Clone the repository
* Copy `config.json.example` to `config.json`
* Run `$ npm install`

## How to use

Run `$ npm start` to start the client
